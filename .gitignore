# Rust
target/
**/*.rs.bk
# NOTE: Cargo.lock is deliberately committed so CI's `--locked` builds are reproducible.

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.envrc
*.env.local

# Logs
*.log

# SQLite
*.db
*.db-wal
*.db-shm
*.db-journal
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm

# Internal docs / dev artifacts (not for public repo)
spec.md
check_list.md
tasks.md

# Python SDK (if built)
__pycache__/
*.egg-info/
dist/
build/
.virtualenvs/
.venv/

# JS/TS SDK (if built)
**/node_modules/
**/tsconfig.*.tsbuildinfo

# Trae IDE / spec staging
.trae/

# Criterion benchmark artifacts
target/criterion/
perf.data
*.perf

# Coverage
coverage/
*.lcov

# SQLx offline cache (regenerated by `cargo sqlx prepare`)
sqlx-data.json
